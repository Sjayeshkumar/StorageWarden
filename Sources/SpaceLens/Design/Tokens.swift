import SwiftUI

enum Spacing {
    static let section: CGFloat = 20
    static let card: CGFloat = 12
    static let tile: CGFloat = 10
}

enum AppPalette {
    static let muted = Color.secondary.opacity(0.85)
    static let border = Color(nsColor: .separatorColor)
}
