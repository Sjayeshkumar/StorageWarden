# App layer

AppSession coordinates saved-index loading, explicit scans, event-driven refresh, and reviewed Trash actions.
ActivityMonitor samples OS counters on its own actor only while its panel is visible.
The internal module name is SpaceLens; the public app name is StorageWarden.
